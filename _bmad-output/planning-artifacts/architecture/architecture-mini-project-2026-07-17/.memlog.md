---
scope: WhatsApp Downloader: worker+dashboard over shared SQLite
purpose: build-substrate
altitude: feature
updated: 2026-07-17T20:27
---

- (constraint) Inherited [ADOPTED] from PRD/addendum: two-process paradigm - always-on Baileys worker + Next.js dashboard, NO direct calls between them; SQLite is the sole integration seam.
- (constraint) Inherited [ADOPTED]: worker is sole writer to SQLite; dashboard reads only (except whitelist edits). Worker queries whitelists live per message (no startup cache) - supports FR-11.
- (constraint) Inherited [ADOPTED] safety invariants from PRD FR-6/7/16/17/18: fail-closed default, staging->scan->final ordering, SSRF/redirect guard with pinned resolved IP, recursive extraction guards, re-scan extracted before final, scanner live+fail-closed, content/filename integrity.
- (version) Verified 2026-07-17: Node 24 Active LTS; TypeScript; Baileys latest=7.0.0-rc13 (RC, breaking) -> pin stable 6.7.x for pilot [ASSUMPTION, revisit if 7.x GA]; Next.js 16.2.10 (LTS 16); better-sqlite3 12.11.x.
- (decision) AD paradigm: pipes-and-filters pipeline worker + shared-DB integration; two processes, SQLite sole seam.
- (decision) AD single-writer: worker is sole writer of pipeline state (events/file-status/auth); dashboard writes ONLY whitelist tables (contacts, link_patterns). Prevents write races + schema-ownership ambiguity.
- (decision) AD sqlite WAL + busy_timeout; dashboard read connections. Prevents SQLITE_BUSY/lost writes under worker<->UI contention.
- (decision) AD schema ownership: worker owns schema + runs versioned migrations at startup; dashboard never DDLs. Prevents incompatible dual migration.
- (decision) AD pipeline = explicit state machine; every item exactly one status from fixed enum; transitions one-directional toward terminal. Prevents ad-hoc status strings + ambiguous 'done'.
- (decision) AD fail-closed transition: any error/uncertain outcome edges to quarantine/hold, never to final/delivered; only explicit positive scan advances. Prevents new step defaulting to pass.
- (decision) AD filesystem-as-state: staging/final/quarantine/extraction-isolated are distinct roots; file dir must match DB status; atomic rename moves; nothing in final without passed scan. Prevents movers disagreeing on location-per-state.
- (decision) AD single guarded fetcher: all outbound fetches route through one component enforcing SSRF/redirect re-check/pinned-IP/streaming-cap. Prevents a second unguarded fetch path.
- (decision) AD secrets+session: secrets from gitignored .env; Baileys auth in dedicated store (NOT useMultiFileAuthState), worker-owned. Prevents dashboard touching session/secrets.
- (decision) AD dedup identity: dedup on content sha256 (primary) + source URL; processed identities recorded. Prevents double-download/deliver on resend/restart. [ASSUMPTION resolves OQ dedup-basis]
- (decision) AD notification side-effect: Telegram send only at terminal transitions; delivery failure recorded as event, never blocks/reverses a state transition.
- (decision) AD link-pattern grammar: exact-domain (+optional path prefix) and/or extension allowlist; NO regex, NO wildcard TLD. Prevents over-permissive wildcard gate. [resolves OQ pattern-grammar]
- (decision) AD bounded concurrency: worker processes downloads via bounded queue (max concurrent + per-sender rate cap). Prevents resource exhaustion/abuse. [ASSUMPTION default N=2, resolves OQ rate/concurrency]
- (event) Fast-path draft starting: 14 ADs + conventions + stack + structural seed.
- (event) Reviewer Gate: lint clean; version-check PASS (2 label fixes: TS 5.9, qrcode 1.5.4, baileys unscoped pkg, file-type ESM); rubric ADEQUATE (op-envelope gap); adversarial-divergence NOT-READY (4 crit: schema seam unpinned, failed status missing, move/write order+recovery, dedup pre-download key).
- (change) Applied all gate criticals+highs: added AD-14 (pinned items/events schema contract), AD-15 (fail-closed startup reconciliation), AD-16 (operational envelope: supervisor+backup+retention). Fixed AD-2 (whitelist no-cache), AD-3 (per-file lock reasoning), AD-5 (added 'failed', removed 'delivered' - delivery is Event), AD-7 (fixed move/write order), AD-10 (pre-download URL dedup key), AD-12 (shared matcher module). Stack version labels corrected. ERD: items+events, auth_state removed to worker store.
- (event) spine finalized
- (change) AD-2 amended: dashboard now also writes a new settings table (in addition to contacts/link_patterns). Worker remains sole writer of items/events.
- (change) AD-14 amended: shared contract adds settings table (key/value, seeded defaults).
- (decision) AD-17: policy values live in dashboard-editable settings store, seeded with defaults, read LIVE by worker (no restart, same as whitelists); secrets remain in .env (AD-9), never in settings. Concrete defaults: download 200MB / uncompressed 500MB / files 1000 / nesting 3 / redirect hops 5 / sig-age 48h / concurrent 2 / rate 10-per-min / VT-flag hard-fail / VT-outage hold / retention 90d / backup daily / supervisor pm2 (deploy).
- (decision) Held-file (VT-outage) needs a release/retry path: re-check when VT returns or manual release in dashboard.
- (event) Spine amended for settings: AD-2 (+settings write), AD-14 (+settings table), new AD-17 (dashboard-editable policy store w/ seeded defaults, live read, secrets excluded), capability map + Deferred updated. lint clean expected.
