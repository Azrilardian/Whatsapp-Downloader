---
topic: WhatsApp Downloader — safe trusted-source file pipeline
updated: 2026-07-17T20:27
---

- (direction) Adopt spec from finalized PRD+addendum+architecture-spine (all this session). Express/direct distill, no elicitation - input is rich & pre-sorted. Slug=whatsapp-downloader.
- (decision) Adopted companions (bmad-spec does not edit): PRD (FR line-item detail + open questions), ARCHITECTURE-SPINE.md (16 ADs), PRD addendum (mechanism/tech). No spec-authored companions - diagrams+glossary already live in adopted spine/PRD.
- (capability) CAP-1 Gated ingestion (FR-1/2/11): only whitelisted-sender + whitelisted-pattern URLs in message text advance; whitelists live-editable.
- (capability) CAP-2 Safe acquisition (FR-3/4/5/16): validate+dedup+fetch into staging via single guarded fetcher (SSRF/redirect/streaming-cap).
- (capability) CAP-3 Scanning+quarantine (FR-6/7/8): live fail-closed scan before trust; archives extracted-after-pass under bomb/zip-slip guards, contents re-scanned.
- (capability) CAP-4 Delivery+notification (FR-9/10): Telegram outcome notices; <=50MB delivered directly, larger/failed as status; no silent failure.
- (capability) CAP-5 Control dashboard+event log (FR-11/12/13/14): local UI for whitelist mgmt + connection status/event log/quarantine/QR re-pair.
- (capability) CAP-6 Connection resilience (FR-14/15): transient drops auto-recover w/ backoff+cap; logout stops+surfaces re-pair; no ban-risky retry.
- (capability) CAP-7 Fail-safe+integrity (FR-17/18): fail-closed on unsafe/incomplete; classify by real bytes, sanitize names, never executable.
- (note) Constraints lifted: whitelist both-sides & narrow-by-design; secondary number only (ban permanent); URL-in-text-only; fail-closed+staging->scan->final; single guarded fetcher; two-process w/ SQLite sole seam (worker sole writer); local single-operator; 50MB Telegram ceiling.
- (event) Self-validate Pass 1 (Coherence, Spec Law 1-6/8): PASS. All 7 CAPs have intent+success; constraints each rule something out; 5 non-goals; success signal testable (EICAR always quarantined); CAP-1..7 unique/stable; lean.
- (event) Self-validate Pass 2 (Preservation): PASS. All 18 FRs map to CAP-1..7; 3 SMs -> success signal; PRD/addendum/spine adopted as companions (downstream reads); brief absorbed to sources. No load-bearing drop. 4 assumptions + 6 open questions carried.
- (event) spec finalized
- (decision) OQ3 reputation-outage: HOLD the file when VirusTotal is unreachable (do not proceed on ClamAV alone). Note: makes VT effectively required when enabled; needs a release/retry path for held files.
- (decision) OQ5 banned-number procedure: SKIPPED for now — remains a deferred operational note, no code decision.
- (decision) OQ6 secondary-number + Telegram setup: DEFERRED — user will provide later; treat as runtime config (dashboard captures Telegram chat_id, number chosen at pairing). Not a build blocker.
- (decision) OQ1 resolved (pilot defaults): max_download=200MB, max_uncompressed=500MB, max_file_count=1000, max_nesting_depth=3, max_redirect_hops=5, scanner_sig_max_age=48h, max_concurrent=2, per_sender_rate=10/min.
- (decision) OQ2 resolved: VirusTotal-flag on ClamAV-clean file = HARD-FAIL to quarantine (fail-closed).
- (decision) OQ4 resolved: process supervisor=pm2 (deploy-level), backup=daily to separate location, events retention=90 days.
- (decision) NEW capability CAP-8: dashboard Settings surface exposes all policy values (sizes/counts/nesting/hops/freshness/concurrency/rate + VT-flag/VT-outage policies + retention/backup cadence), seeded with defaults, worker reads live (no restart). Secrets (Telegram/VT keys) stay in .env, NOT settings. Local single-operator trust model (NFR-6).
- (event) SPEC re-derived: added CAP-8 (policy settings), Resolved Policy Defaults section, trimmed Open Questions to 2 deferred. Self-validate PASS (8 CAPs intent+success; preservation intact).
